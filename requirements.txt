# GUI library
tkinter

# PDF generation library
reportlab              # For generating PDFs
fpdf2                  # Alternative lightweight PDF library

# Database (SQLite is built-in, no external dependency)
pandas

# Testing tools (add later if needed)
pytest